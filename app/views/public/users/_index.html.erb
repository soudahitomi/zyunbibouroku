ユーザー一覧ですよ
検索で呼び出す共通ファイル

     <% users.each do |user| %>
        <%= image_tag user.get_profile_image(50,50) %>
        <%= user.name %>
        フォロー数: <%= user.followings.count %>
        フォロワー数: <%= user.followers.count %>

         <%#= link_to "Show", user %>
     <% end %>